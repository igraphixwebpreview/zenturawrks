# SMTP Email Configuration for Invoice Generator
# Copy this file to .env and configure with your email provider settings

# Gmail SMTP Settings (recommended for most users)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=your-email@gmail.com

# Alternative providers:
# 
# Outlook/Hotmail:
# SMTP_SERVER=smtp-mail.outlook.com
# SMTP_PORT=587
#
# Yahoo Mail:
# SMTP_SERVER=smtp.mail.yahoo.com
# SMTP_PORT=587
#
# Custom SMTP Server:
# SMTP_SERVER=mail.yourdomain.com
# SMTP_PORT=587

# Instructions:
# 1. For Gmail: Use an App Password instead of your regular password
#    - Go to Google Account settings > Security > 2-Step Verification > App passwords
#    - Generate an app password for "Mail"
#
# 2. For Outlook: Use your regular email and password
#
# 3. For other providers: Check your email provider's SMTP settings